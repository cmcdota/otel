server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }

  # Jaeger UI ходит за данными в /api/*
  # мы проксируем это в VictoriaTraces Jaeger Query API
  location /api {
    proxy_pass http://victoria-traces:10428/select/jaeger/api;
  }
}
